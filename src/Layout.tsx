import { Outlet } from 'react-router-dom';

// ==============================|| LAYOUT ||============================== //

const Layout = () => (
  <>
    <Outlet />
  </>
);

export default Layout;
